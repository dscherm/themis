{
  "_comment": "Settings for Blind TDD Agent #3 (arbiter). Ruling-only agent. Read-only access to task spec, public_api.md, the specific disputed test, and the challenge text. No Bash, no Edit. Write is allowed SO THE ARBITER CAN EMIT ITS MANDATED ruling — the path guard scopes it to rulings/** and denies writing any sealed test file. Edit/NotebookEdit stay denied.",
  "permissions": {
    "allow": [
      "Read",
      "Grep",
      "Glob",
      "Write",
      "WebFetch"
    ],
    "deny": [
      "Bash",
      "Edit",
      "NotebookEdit"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Grep|Glob|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/blind_tdd_path_guard.py"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python .claude/hooks/blind_tdd_audit.py"
          }
        ]
      }
    ]
  }
}
